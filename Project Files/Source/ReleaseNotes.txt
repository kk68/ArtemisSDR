# ArtemisSDR

A native SDR host for the SunSDR2 DX, dedicated to the Artemis II mission. Forked from Thetis by Richard Samphire (MW0LGE), itself descended from OpenHPSDR (Doug Wigley, W5WC) and PowerSDR (FlexRadio Systems). Released under GPL v2.

Report issues at: https://github.com/kk68/ArtemisSDR/issues

---

## Beta v1.0.3 — Flag fix + setup-doc clarity

**Recommended upgrade for everyone on v1.0.2.** Same binary functionality as v1.0.2 — this release exists primarily to push the country-flag fix to anyone who installed the original v1.0.2 MSI before it was hot-patched on 2026-04-17.

### Fixed

- **Country flags in front of DX-cluster spot callsigns now render correctly on the panadapter.** The original v1.0.2 MSI shipped with broken flag rendering — every callsign came up flag-less, even with a working TCI feed. Two underlying causes (both addressed):
  1. *TCI wire identifier:* during the rebrand, four wire-level TCI protocol strings had been renamed from "Thetis" to "ArtemisSDR". DX-cluster apps and loggers match on the literal "Thetis" to know whether to send rich JSON spot payloads (which carry the flag info). Reverted those four wire strings to "Thetis" — they are protocol-level only, never user-visible.
  2. *BinaryFormatter assembly resolution:* the bundled `cty` country-data resource is a binary blob serialized when the assembly was named "Thetis", referencing `List<Thetis.CountryData+PrefixData, Thetis, ...>` in its type header. After AssemblyName changed to "ArtemisSDR", BinaryFormatter could no longer resolve the old "Thetis" assembly when loading generic type arguments → CountryData's static initializer silently caught a `FileNotFoundException` and left its prefix list null → every callsign lookup returned null → no flags. Added an `AppDomain.AssemblyResolve` handler at startup that redirects requests for the pre-rebrand "Thetis" assembly to the currently-executing ArtemisSDR assembly. Also extended the BinaryFormatter type-rename binder to cover the same redirect for any saved user database carried over from a pre-rebrand install.

### Documentation

- **`START_HERE_SUNSDR2DX.md` rewritten with a screenshot-driven manual-add walkthrough.** Auto-discovery does not work for the SunSDR2 DX in ArtemisSDR — every user must add the radio manually as a Custom entry. The new Step 3 walks through it click-by-click with three screenshots: (3a) tick `Advanced` and click `Custom`; (3b) fill in the Add Custom Radio dialog with NIC + IP:Port; (3c) select the new entry and click `Apply`. A strongly-worded callout explains the two distinct ports (`Fixed listen port` for control, `Radio IP:Port` for discovery/data) and that **both must match what the radio is actually configured for**. Also calls out picking the correct `Via NIC` (the LAN adapter on the same subnet as the radio) — the most common reason a manual add fails.
- README "Troubleshooting → ArtemisSDR doesn't see the radio" entry rewritten to lead with the manual-add requirement and link directly into the new Step 3 walkthrough.

### Notes

- AppData and registry layout unchanged from v1.0.2 — your settings, skins, and memory lists carry over automatically.
- If you installed the pre-hotfix v1.0.2 MSI (the one originally published 2026-04-16), uninstall it before installing v1.0.3. The mid-day-replaced v1.0.2 MSI on the GitHub release is functionally identical to v1.0.3 and can be upgraded in place.

---

## Beta v1.0.2 — Legal and documentation polish

Same binary functionality as v1.0.1 with a thorough pre-public-release cleanup pass:

- Added `MODIFICATIONS.md` documenting ArtemisSDR changes to upstream files (GPL v2 §2(a) compliance)
- Added `THIRD_PARTY_LICENSES.md` with full attribution roll-up for every bundled library
- Added `Privacy & network activity` section to README documenting the version-check and skin-server HTTPS calls, AppData writes, firewall rules, and registry state
- About dialog now displays the warranty disclaimer and redistribution notice required by GPL v2 §2(c)
- K0KOZ added to the About-dialog contributors list (was missing in v1.0.1)
- About-dialog third-party attribution expanded to include WDSP, fftw, rnnoise, libspecbleach, NAudio, Markdig, HtmlAgilityPack, WindowsFirewallHelper, System.Reactive
- Reverse-engineering language corrected from "clean-room" to "black-box" — more accurate characterisation of solo-developer protocol observation, preserving DMCA §1201(f) interop safe harbor
- Third-party prebuilt installer EXEs removed from source tree (`MeterSkinInstaller.exe`, `OpenHPSDR_Skins.exe`) — licence provenance unclear; users who want upstream Thetis skins can fetch them separately
- Hardcoded list of 33 upstream-tester callsigns removed from `setup.cs` (they consented to being in upstream Thetis, not this fork)
- ExpertSDR non-affiliation statement added to the About-dialog text
- README rephrased to remove comparative-superlative phrasing against ExpertSDR
- Lab-IP `10.0.3.50` in README troubleshooting replaced with a generic placeholder
- Dangling `SUNSDR_RASPY_TX_DEBUG.md` reference cleaned up in START_HERE
- GPL v2 file headers restored on `NetworkIO.cs` and `clsDiscord.cs`
- Splash screen version line now reads `ArtemisSDR <version> SunSDR2 DX`
- Tooltip typo fixed in About dialog ("Visit he" → "Visit the")
- `sunsdr.c` / `sunsdr.h` copyright updated to full name `Kosta Kanchev (K0KOZ)`
- `.gitignore` tightened (`*.bak`, `*.raw`, `.env`, `sunsdr_debug.log`, etc.)
- Repo backup (`.bak`) files removed, duplicate `LICENSE-DUAL-LICENSING.rtf` removed, duplicate manual PDF under `Documentation/Radio/` removed

No functional changes; recommended upgrade for all v1.0.1 users. Uninstall v1.0.1 before installing v1.0.2.

---

## Beta v1.0.1 — First public release

First ArtemisSDR release under its own name and version line. Speaks the SunSDR2 DX's native UDP protocol directly, with no ExpertSDR proxy, bridge, or firmware changes.

### Receive
- RX1 with panadapter, waterfall, and audio on every mode ArtemisSDR supports
- RX2 as an independent second receiver with its own VFO B and audio path
- RX antenna selection (primary / auxiliary inputs)
- Live firmware version and radio serial number display in the title bar and Setup → H/W Select

### Transmit
- MOX and TUNE via native wire protocol — reliable across consecutive attempts (11/11 TUNE and 10/10 voice MOX verified)
- TUNE in every mode lands on the dial frequency: SSB, CW, AM, FM
- Voice SSB, AM, CW, and digital modes transmit on the correct sideband / carrier
- External PA (xPA) control during MOX and TUNE
- TX power calibrated linearly on 40 m — drive slider in watts maps to actual RF within ~1 W across 10-90 W
- TX antenna selection (primary / auxiliary outputs)
- AM Carrier Level setting wired through to the WDSP AM modulator

### General
- Power-on to RX in ~1-1.5 seconds, comparable to ExpertSDR3
- Sub-second band switching, native protocol, no session teardown
- All of the WDSP DSP stack: NR, NR4, ANF, NB/NB2, EQ, CESSB, CFC, notch, compander
- VAC audio routing (CABLE, VoiceMeeter, etc.) works on both RX and TX
- Clean power-off / power-on cycling from the ArtemisSDR UI
- Proper `PA Gain By Band` and per-drive offsets integration — calibrate per band the same way you'd calibrate any Thetis-family radio

### System integration — new in this release
- Isolated user data under `%AppData%\ArtemisSDR\` — does not share DB, settings, logs, or skins with any Thetis-for-ANAN install
- Dedicated single-instance mutex — ArtemisSDR and Thetis can run on the same machine without mutual detection
- Windows Firewall rules registered under the ArtemisSDR name
- ArtemisSDR-branded registry paths, Start Menu shortcut, desktop shortcut, and installer

### Known limitations
- **TX power calibration**: 40 m is locked; other bands fall back to the 40 m curve. Expect a few dB deviation until you run a per-band sweep (see README → "TX power calibration per band")
- **Fwd Pwr meter**: not yet wired to the SunSDR telemetry stream. Use an external wattmeter. Planned for the next release.
- **PS-A, 2-TONE, DUP**: grayed out on SunSDR — these depend on a feedback-loop path the radio doesn't expose. Hardware-architecture constraint, not a bug.
- **Diversity mode**: unsupported. RX2 follows RX1's antenna selection.
- **MON / DUP audio routing**: not fully settled during TX. If you need to monitor your own transmission, use a second receiver.
- **Occasional post-TX raspy audio**: intermittent; cycling VAC clears it. Tracked as a polish item.

### Versioning scheme

- **Patch** (1.0.1 → 1.0.2): routine fixes and small features
- **Minor** (1.0.x → 1.1.0): meaningful new feature, batch of related fixes
- **Major** (1.x.y → 2.0.0): architectural change, protocol overhaul, or exit from Beta

### Installation notes

If you are upgrading from a pre-release build of this fork, your previous database location was `%AppData%\OpenHPSDR\Thetis-x64\` — that data is NOT migrated automatically. If you had settings you want to preserve, copy them manually before first launch.

Hardware model selection in Setup is SUNSDR2-DX only. Other Thetis-supported radios (ANAN / Hermes / Orion / Saturn) are hidden from the UI; the underlying code paths remain in place but are not exercised by this build.

---

## Upstream Thetis heritage

ArtemisSDR's DSP stack, panadapter, CAT server, TCI server, metering architecture, and the entire Console framework come directly from Thetis by Richard Samphire (MW0LGE) — which itself traces back through Doug Wigley's OpenHPSDR Thetis fork and, ultimately, to FlexRadio's PowerSDR. Decades of prior work live inside this codebase.

For the full Thetis release history see:
  https://github.com/ramdor/Thetis/releases

For the full PowerSDR / OpenHPSDR heritage see:
  https://openhpsdr.org/
  https://github.com/TAPR/OpenHPSDR-wdsp

73!
